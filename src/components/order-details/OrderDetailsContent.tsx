import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

type Props = {}

const OrderDetailsContent = (props: Props) => {
  return (
    <View>
      <Text>OrderDetailsContent</Text>
    </View>
  )
}

export default OrderDetailsContent

const styles = StyleSheet.create({})